import Rx from 'rxjs';
//
// const getObsForKey = (key) => {}
// const saveObsForKey = (key, obs) => {}
//
// const StorageObserverFactory = (key) => {
//   if (!getObsForKey(key)) {
//     saveObsForKey(key, Rx.Observable.create((subscriber) => {
//
//     }));
//   }
//
//   return getObsForKey(key);
// };

const Storage = {
};

export default Storage;
