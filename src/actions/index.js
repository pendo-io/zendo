import Storage from '../sources/Storage';

export const ToggleMetadataAction = (type, groupName, item) => {
  console.log("everyday toggling", type, groupName, item);

  // get ticketId
  // get ticket storage
  // read metadata-filter for visitor | account
  // construct new filter list (de-dupe)
  // write out
}
